# 週次ログの原本。体調の記述・ヒアリング相手の業種・署名（実名）を含む。
# 公開用JSONはこれらを機械的に除外して生成しているが、原本そのものを
# リポジトリに置くと除外の意味がなくなる。Gitは履歴が残るため、
# 一度コミットすると後から消しても過去のコミットから読める。
# ビルドはローカルで実行し、生成物（data/*.json）だけをコミットする。
logs/

# ローカルのエディタ／開発ツール設定。実行ポートなどが環境依存で、共有する意味がない。
.claude/

node_modules/
npm-debug.log*
.DS_Store
.env
.env.*
